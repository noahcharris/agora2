

module.exports.buildProfile = function(username) {
  //TODO
  //builds the vote profile for the given user

  //when should we build a completely new profile and when
  //should we merely update the old one?

  //but user profile will contain other things besides votes (permissions)
  //so perhaps it should be constructed elsewhere
};

module.exports.rankTopic = function(id) {
  //TODO
};

module.exports.rankComment = function(id) {
  //TODO
};

module.exports.rankReply = function(id) {
  //TODO
};

module.exports.rankGroup = function(id) {
  //TODO
};

module.exports.rankSubgroup = function(id) {
  //TODO
};